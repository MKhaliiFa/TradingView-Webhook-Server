[project]
name = "snpr-mt5-bridge"
version = "0.1.0"
description = "TradingView -> MT5 EA bridge: receives alerts via /webhook and serves them to the EA via /get_signal."
requires-python = ">=3.11"
dependencies = [
    "flask==3.1.1",
    "python-dotenv==1.1.0",
]
